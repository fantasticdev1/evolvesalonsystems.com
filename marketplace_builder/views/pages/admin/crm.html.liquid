---
slug: admin/crm
layout_name: admin/panel
---
{% include 'admin/layout/constants' -%}
{% include 'admin/crm/records' -%}
{% include 'admin/crm/all_fields' -%}
{%- if context.location.search.name -%}
	user detail
{%- else -%}
	<div class="card">
		<div class="card-header">
			<h4 class="card-title">Users</h4>
		</div>
		<div class="card-body">
			<div class="toolbar">
				<!-- Here you can write extra buttons/actions for the toolbar -->
			</div>
			<table class="table table-striped table-bordered" cellspacing="0" width="100%" data-posdk-dtables="resource:model; parent:{{context.location.search.name}}; fields:/admin/crm/fields.json?name={{context.location.search.name}}; records:/admin/crm/records.json?name={{context.location.search.name}}; form:/admin/crm/form.json?name={{context.location.search.name}};">
				<caption><a href="#" class="btn btn-success btn-md add_record">New Record <i class="fas fa-plus add_record"></i></a></caption>
				<tbody></tbody>
			</table>
		</div>
	</div>
{%- endif -%}
