---
slug: admin/crm/fields
layout_name:
format: json
---
{% include 'admin/crm/all_fields' -%}
{%- if fields -%}
[{%- for field in fields -%}{ "label": {{field.name | json}},"name": {{field.name | json}} }{%- unless forloop.last -%},{%- endunless -%}{%- endfor -%}]
{%- else -%}
{"error": "No user with that id was found."}
{%- endif -%}
