{% comment %} <div class="flex-center">
  <div>
    <h1>{{meta.title}}</h1>
    <h2>{{meta.description}}</h2>
    <button class="medium">SIGN IN</button>
  </div>
  <img src="{{meta.imageUrl}}" style="width:500px;">
</div> {% endcomment %}
<div class="order-section container">
  <div class="row"> 
    <div class="left-section col">
      <div class="text-section">
        <h1>{{meta.title}}</h1>
        <p class="description">{{meta.description}}</p>
        <button class="button-wide">SIGN IN</button>
      </div>
    </div>
    <div class="right-section col" style="background-image: url({{meta.imageUrl}});">
      <svg id="bigHalfCircle" xmlns="http://www.w3.org/2000/svg" version="1.1" width="100%" height="100" viewBox="0 0 100 100" preserveAspectRatio="none">
        <path d="M0 100 C40 0 60 0 100 100 Z" />
      </svg></div>
  </div>
</div>